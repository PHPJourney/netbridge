# QA: nbvpn（T06 / S05）

> 日期：2026-08-14（**部分复测**：Linux VPS 真 wg + Store mock 链接）  
> 对照：`docs/dev-workflow/context/`；竖切 `S05-customer-delivery/acceptance.md`；回归 S02/S03/S04；`01-spec.md`；`04-impl-notes.md`  
> 环境：macOS darwin 24.6；**Debian 12 VPS** `114.66.58.141`（SSH 别名 `netbridge-vps`）；无 Android/iOS 实机

## 范围与环境

| 项 | 值 |
|----|-----|
| 测什么 | 本机单测 + Store 构建预览 + **Debian VPS 真 WireGuard** 安装/冒烟/启停/peer |
| 不测什么（诚实） | 四端实机扫码互通；OpenList **真**下载；签名安装包；云控制台安全组外层（仅验本机 ufw） |
| 工具版本 | Go 1.23 / Flutter / Node Vite 6 |

### 已执行命令 / 证据

| # | 动作 | 结果 |
|---|------|------|
| 1 | `cd server/nbvpn && go test ./...` | **PASS** |
| 2 | Dry-run（历史）+ **VPS** `install.sh` | **PASS** — Debian 12；`wg-quick@nbvpn` active；endpoint `114.66.58.141:51820` |
| 3 | VPS `smoke-verify.sh` | **PASS=12 FAIL=0 SKIP=0** |
| 4 | VPS `peer add` / `revoke` / `stop|start|restart` / `wg show` | **PASS** — 热同步；revoke 后接口无该 peer |
| 5 | `ufw allow 51820/udp` | **PASS**（此前未放行，已补） |
| 6 | `cd clients/netbridge && flutter test` | **PASS**（12 tests，含 URI sanitize） |
| 7 | VPS URI 本机 JSON 解析 | **PASS**（endpoint/v=1；未入库密钥） |
| 8 | `cd apps/store && npm run build` + `preview` | **PASS** → `http://127.0.0.1:4173/`；`releases.json` `mock_links` |
| 9 | Android/桌面真连 VPS | **未跑** — 无手机；macOS 仍 Stub |

---

## 对照本刀 acceptance（S05）

| ID | 结果 | 证据 | 备注 |
|----|------|------|------|
| AC-01 E2E 主路径 | **partial / blocked** | 服务端真 wg OK；无四端实机 | 客户端导入路径已文档化 |
| AC-02 安全基线 | **partial** | CLI 不泄服务端私钥；Store 无登录 | mock SHA；请轮换曾分享的 root 密码 |
| AC-03 文档齐套 | **partial** | README/IMPL/规格更新 | 缺 `06-release.md`（T07） |
| AC-04 QA 门禁 | **fail** | 仍有 blocker | 无客户书面接受 |
| AC-05 交付清单 | **fail** | mock 链接≠真包；无签名四端包 | |

---

## 用例与结果

### S02 / SV（服务端）— 本轮 VPS 复测

| 验收 ID | 结果 | 证据 | 备注 |
|---------|------|------|------|
| S02 AC-01 / SV-01 一键安装 | **pass（Debian 12）** | `install.sh` + status up | Ubuntu/RHEL/CentOS 矩阵未跑 |
| S02 AC-02 / SV-03 终端二维码 | **pass（结构）** | smoke-verify 块字符 + PNG | 手机实扫未做 |
| S02 AC-03 / SV-04 URI/文件 | **pass** | show --uri/--file + PNG 0600 | |
| S02 AC-04 / SV-05 config | **pass** | 无服务端私钥 | |
| S02 AC-05 / SV-06 服务管理 | **pass** | stop→inactive；start/restart→active | |
| S02 AC-06 傻瓜式 | **pass** | CLI + 安装提示含 ufw | |
| SV-02 四系覆盖 | **partial** | 仅 Debian | |
| SV-07 吊销 | **pass（接口侧）** | revoke 后 `wg show` 无该 peer | 客户端旧 profile 未实机验证 |
| SV-08 错误可读 | **partial** | 未注入端口占用 | |

### S03 / CL（客户端）

| 验收 ID | 结果 | 证据 | 备注 |
|---------|------|------|------|
| CL-02 URI | **pass（单测+VPS 解析）** | sanitize + decode | UI 实机粘贴未测 |
| CL-06 四端连通 | **fail / blocked** | 无设备；Apple Stub | Android 理论上可连 VPS |
| 其余 | 同前稿 | | |

### S04 / ST（Store）

| 验收 ID | 结果 | 证据 | 备注 |
|---------|------|------|------|
| ST-01～06 | **pass / partial** | mock 后版本/SHA/链接可点 | mock URL 会 404，仅 UI |
| ST-07 校验和 | **partial** | 服务端 sha=真交叉编译；客户端 sha 为 mock 图案 | 非 OpenList 真包 |

---

## 缺陷列表

| 编号 | 严重度 | 复现 / 描述 | 状态 |
|------|--------|-------------|------|
| DEF-QR | minor（残留） | 窄 SSH 折行 | 用 PNG/`--uri` |
| DEF-01 | **major**（原 blocker 降级） | OpenList 真直链未上传；现为明确 **mock_links** | 开放 |
| DEF-02 | **blocker** | iOS/macOS Extension + Team | 开放 |
| DEF-03 | ~~blocker~~ | 无 Linux VPS | **已关闭**（Debian 12 真 wg） |
| DEF-04 | **major** | 四端签名包未产出 | 开放 |
| DEF-05 | **major** | Kill Switch 平台不齐 | 开放 |
| DEF-06 | **minor** | Windows 无相机扫码 | 已知限制 |
| DEF-FW | **major**（运维） | 安装后 ufw 默认未放行 51820/udp | **本 VPS 已修**；云安全组待确认 |

---

## 结论

**不通过** — **T07 Release：NO-GO**

进步：DEF-03 关闭；S02 在 Debian 真机大体通过；Store UI 可展示可点 mock 链接。  
仍阻客户交付：真 OpenList 包、Apple 真隧道、四端实机 E2E、签名产物。

### 安全提醒

聊天中曾明文分享 VPS root 密码 — **请立即轮换**；开发机已改用 `netbridge-vps` 密钥登录。

签字人 / 日期：_（待填）_

---

## 修订记录

| 日期 | 变更 | 作者 |
|------|------|------|
| 2026-08-14 | T06 初稿；T07 no-go | qa |
| 2026-08-14 | VPS 真 wg 复测；Store mock；DEF-03 关闭；T07 仍 NO-GO | qa/impl |
